import 'package:flutter/material.dart';

class CarOwnerInformation extends StatelessWidget {
  const CarOwnerInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}